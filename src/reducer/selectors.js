export const getOffers = (state) => state.offers;

export const getShowingOffersCount = (state) => state.showingOffers;

export const getFilters = (state) => state.filters;

export const getCategories = (state) => state.categories;

export const getIsShowCategories = (state) => state.isShowCategories;

export const getIsShowFilter = (state) => state.isShowFilter;

export const getActiveCategory = (state) => state.activeCategory;

export const getActiveFilter = (state) => state.request;
